"use client";
import { PackGuideView } from "@/components/help/PackGuideView";

/** The tenant's installed-pack solution guide (overview + per-persona day-to-day). */
export default function HelpPackPage() {
  return <PackGuideView />;
}
