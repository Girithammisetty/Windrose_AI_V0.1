# Pipeline seeds (CMM-FR-033 enrollment propensity, expressed against the real
# pipeline-orchestrator algorithm catalog): an enrollment-propensity classifier
# trained on the Medicare panel's consent-vs-offered outcomes — the ranking
# input for the Enrollment Eligibility Scanner (CMM-FR-030). The nightly
# enrollment-scan / label-export and quarterly code-selection distillation
# Argo workflows (CMM-FR-150..152) are deferred — they require distillation
# endpoints not yet shipped in Core.
- identity: enrollment_propensity
  name: "Enrollment propensity classifier (xgboost)"
  algorithm: xgboost
  mode: train
  dataset: care_patients
